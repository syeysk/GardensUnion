[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gardensunion"
version = "0.0.1"
requires-python = ">=3.12"
description = "The base system to link different objects"
